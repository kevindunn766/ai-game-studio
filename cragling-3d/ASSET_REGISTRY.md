# Cragling — Asset Registry

Living document, per `../cragling-design-brief.md`'s Asset Registry section. Every stand-in asset gets a row the moment it's created, one per asset, no batching. Columns are locked; final art direction is an explicit later decision, deferred until gameplay is polished.

| ID | Band | Role | Stand-in | Intended final look (notes for future art pass) | Status |
|---|---|---|---|---|---|
| CRAGLING_HEAD_01 | n/a | player | Tan `BoxMesh`, `Climber/Torso/Ribs/Head` | Sculpted head with simple face | Built |
| CRAGLING_HAIR_01 | n/a | player (cosmetic) | Brown `BoxMesh` blob, `Climber/Torso/Ribs/Hair` | Styled hair tuft, per-cosmetic color variants | Built |
| CRAGLING_RIBS_01 | n/a | player | Green `BoxMesh` (0.5x0.34x0.3), `Climber/Torso/Ribs/RibMesh` — upper torso box; arms, head, hair, backpack all hang off it; twists up to ±10° toward whichever arm led most recently (`Climber._process`) | Textured shirt/jacket | Built |
| CRAGLING_HIPS_01 | n/a | player | Darker olive `BoxMesh` (0.42x0.3x0.28), `Climber/Torso/Hips/HipMesh` — lower torso box; legs hang off it; counter-twists opposite the ribs at 0.6× the ribs' angle | Textured shorts/harness | Built |
| CRAGLING_WAIST_01 | n/a | player | Thick chain of 5 wide `BoxMesh` segments along a per-frame `Curve3D` linking rib-bottom to hip-top, `waist_spline.gd` (same box-chain technique as limbs, no IK) | Flexible waist/midsection mesh or cloth sim | Built |
| CRAGLING_BACKPACK_01 | n/a | player | Orange `BoxMesh`, `Climber/Torso/Ribs/Backpack` | Detailed pack with straps, gem pouch | Built |
| CRAGLING_LIMB_01 | n/a | player | Chain of 6 thin `BoxMesh` segments sampled along a per-frame `Curve3D`, positioned by a 2-bone analytic IK solve (law of cosines, fixed `UPPER_LENGTH`/`LOWER_LENGTH`) in `limb.gd` — one instance each for left/right arm and leg. Elbow/knee fold limited to a researched min interior angle (30°/40°) via the inverse law of cosines, not just an arbitrary distance floor. Elbow/knee position also: (1) raycast-checked against the wall's real `CollisionShape3D` every frame (`_clamp_to_wall`) and pulled to just in front of the actual hit surface if it would clip through; (2) pushed apart from the other 3 limbs' elbow/knee points (cheap sphere-vs-point, `_resolve_sibling_separation`); (3) pushed out of the torso boxes, approximated as bounding spheres (`_resolve_body_avoidance`) | Smooth continuous limb mesh or rigged bone chain | Built |
| CRAGLING_HAND_01 | n/a | player | Small `BoxMesh` at each arm's tip, oriented by bending the forearm's final direction toward the grip surface normal, capped at a researched wrist ROM (70°), `limb.gd::_place_hand` | Sculpted hand gripping the hold, fingers curled | Built |
| CRAGLING_FOOT_01 | n/a | player | Same technique as CRAGLING_HAND_01 at each leg's tip, capped at a researched ankle ROM (40°, smaller than the wrist's per real anatomy) | Sculpted climbing shoe | Built |
| WALL_SLOPE_01 | 1 (prototype, unbiomed) | climbing surface | Single grey-brown `BoxMesh` + `BoxShape3D`, `Wall` node | Sculpted rock face mesh with real handholds, per-biome texture | Built |
| GRIP_MARKER_01 | 1 (prototype, unbiomed) | grip point (visual debug) | Small grey `SphereMesh`, one per generated non-crumbling grip in `grip_field.gd`, Poisson-disc placed (guaranteed `min_distance` between any two grips) | Removed or replaced by visible rock ledges/roots/crystals once grips are read directly off sculpted geometry | Built |
| GRIP_CRUMBLE_01 | 1 (prototype, unbiomed) | obstacle (crumbling hold) | Small orange `SphereMesh` variant, same Poisson-disc field, `crumble_chance` roll in `grip_field.gd`; breaks away (scale-out tween + free) after `crumble_uses` landings via `Climber._execute_path` → `GripField.consume_grip()` | Visibly cracked rock texture that flakes/crumbles on use, debris particles on break | Built |
| CRAGLING_ROPE_01 | n/a | player (descent visual) | Chain of 8 thin gold/tan `BoxMesh` segments along a per-frame `Curve3D`, `rope_spline.gd` — same box-chain technique as limbs/waist, no IK. Reworked from an earlier version whose bottom endpoint followed the tap/click position (diagonal, tap-dependent) after that was flagged as wrong: the rope is now always perfectly vertical, top pinned to whichever currently-held grip is highest, straight down (same X/Z, no lean) to `ROPE_BOTTOM_Y`, entirely independent of where the player tapped. Set once per descent gesture in `Climber.start_rappel_to`/`start_continuous_slide`, not redrawn every frame. First color pick (muted brown) was visually indistinguishable from the grey limb segments in an actual screenshot under this scene's lighting; corrected to a clearly saturated gold rather than trusting the "should be different enough" assumption | Textured braided rope, frayed ends | Built |
| GROUND_01 | n/a | prop (backdrop) | Flat dark `BoxMesh`, `Ground` node | Textured base-camp ground | Built |

No waterfall-curtain / wind / ice obstacle, gem, or cave-puzzle rows yet — deferred to their own Build Milestones per `../cragling-design-brief.md`. Crumbling holds currently have no consequence for a limb standing on one when it breaks (the limb just keeps its last logical grip); real fall/recovery behavior belongs to the brief's separate Fail State design, not yet built.
